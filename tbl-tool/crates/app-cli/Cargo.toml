[package]
name = "tbl-cli"
version = "0.1.0"
edition = "2021"

[lib]
name = "tbl_cli"
path = "src/lib.rs"

[[bin]]
name = "tbl-cli"
path = "src/main.rs"

[dependencies]
tbl-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
chrono = "0.4"
toml = "0.8"
